1.发送邮件代码，由于不会配置到 RPWcommon文件中，导致在定时的两个项目中都，重新写了一根邮件发送代码；
2.用户名，哪里用了DES加密算法。使用户名在，url中传递；
3.这样，没有使用Server.Url来编码，主要是，会出现Current为null的情况；不知道，异常怎么发生的，所以，使用了DES加密算法；
4.消息队列，只有用户名激活，和新用户注册，发送给系统管理员；其他的重置密码通知，没有做；